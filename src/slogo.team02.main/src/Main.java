public class Main {

    public static void main(String[] args) {

        // TODO: to add dependencies between packages
        var m = new Main();

        // set up the parser
        //var lang = new ProgramParser();
        // these are more specific, so add them first to ensure they are checked first
//        lang.addPatterns("regex/resources/languages/English");
//        lang.addPatterns("regex/resources/languages/Syntax");
//
//        // try against different inputs
//        // note, this simple "algorithm" will not handle SLogo comments
//        m.parseText(lang, m.examples);
//        String userInput = "fd 50 rt 90 BACK :distance Left :angle";
//        m.parseText(lang, userInput.split(WHITESPACE));
//        String fileInput = m.readFileToString("/regex_example/data/square.logo");
//        m.parseText(lang, fileInput.split(WHITESPACE));
    }
}
